apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      name: redis
  template:
    metadata:
      labels:
        name: redis
        app: demo-voting-app
    spec:
      containers:
      - name: redis
        image: redis
        imagePullPolicy: Always
